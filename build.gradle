apply plugin: 'java'

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile 'org.apache.velocity:velocity:1.7'
}

dependencies {
    compile(name: 'velocity-1.7', ext: 'jar')
}
repositories {
    flatDir {
        dirs 'libs'
    }
}

sourceCompatibility = "1.7"
targetCompatibility = "1.7"
